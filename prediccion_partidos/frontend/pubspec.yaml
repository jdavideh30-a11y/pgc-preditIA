name: prediccion_partidos
description: Plataforma de Prediccion de Partidos con Inteligencia Artificial
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  # HTTP y estado
  http: ^1.2.0
  provider: ^6.1.1
  shared_preferences: ^2.2.2
  # UI
  fl_chart: ^0.67.0
  google_fonts: ^6.1.0
  # Utilidades
  intl: ^0.19.0
  flutter_secure_storage: ^9.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/
